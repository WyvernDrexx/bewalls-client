query SearchTextString($searchText: String!) {
  search(searchText: $searchText) {
    wallpapers {
      name
      imageUri
      downloads
      id
    }
  }
}

query ExtrasData {
  colors {
    id
    name
    code
  }
  hotSearches {
    term
  }
}

query Recommended {
  recommended {
    name
    imageUri
    downloads
    id
  }
}
