query wallpapers(
  $bundleId: String
  $categoryId: String
  $tagsId: String
  $colorId: String
) {
  wallpapers(
    data: {
      bundleId: $bundleId
      categoryId: $categoryId
      tagsId: $tagsId
      colorId: $colorId
    }
  ) {
    name
    imageUri
    downloads
    id
    sizeInKB
    height
    width
    category {
      name
      id
    }
    tags {
      name
      id
    }
  }
}
